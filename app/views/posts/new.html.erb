<h1>Create a new post</h1>
<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.hidden_field :user_id, value: @current_user.id %>

  <%= f.submit "Create post" %>
<% end %>
